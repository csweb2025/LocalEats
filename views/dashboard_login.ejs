<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LocalEats - Dashboard Login</title>
  <link rel="stylesheet" href="/tailwind.css" />
</head>
<body class="bg-gradient-to-br from-gray-50 to-white min-h-screen">
    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8">
            <div class="text-center">
                <a href="/" class="inline-block text-3xl font-extrabold text-orange-600">LocalEats</a>
                <p class="mt-2 text-sm text-gray-600">Panou administrare restaurante</p>
            </div>

            <div class="bg-white py-8 px-6 shadow rounded-lg">
                <h2 class="mb-6 text-center text-2xl font-semibold text-gray-900">Autentificare Dashboard</h2>

                <% if (typeof error !== 'undefined') { %>
                    <div class="mb-4 rounded-md bg-red-50 p-3 text-red-700 text-sm">
                        <strong class="font-semibold">Eroare:</strong> <%= error %>
                    </div>
                <% } %>

                <form action="/dashboard/login" method="POST" class="space-y-5">
                    <div>
                        <label for="username" class="block text-sm font-medium text-gray-700">Nume de utilizator</label>
                        <input type="text" name="username" id="username" required class="mt-1 block w-full px-3 py-2 border border-gray-200 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500 bg-gray-50">
                    </div>

                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700">Parolă</label>
                        <input type="password" name="password" id="password" required class="mt-1 block w-full px-3 py-2 border border-gray-200 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500 bg-gray-50">
                    </div>

                    <div>
                        <button type="submit" class="w-full py-2 px-4 rounded-md text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500">Autentificare</button>
                    </div>
                </form>
            </div>

            <p class="text-center text-sm text-gray-500">© <%= new Date().getFullYear() %> LocalEats</p>
        </div>
    </div>
</body>
</html>